import state from "../state/state_<%= @underscore_name %>";
import getters from "../getters/getters_<%= @underscore_name %>";
import mutations from "../mutations/mutations_<%= @underscore_name %>";
import actions from "../actions/actions_<%= @underscore_name %>";

export default {
  state,
  getters,
  mutations,
  actions,
};
